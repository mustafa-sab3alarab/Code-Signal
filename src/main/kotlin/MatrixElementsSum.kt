fun main() {
    val matrix = mutableListOf(
        mutableListOf(0, 1, 1, 2),
        mutableListOf(0, 5, 0, 0),
        mutableListOf(2, 0, 3, 3)
    )
    matrix.forEach { row ->
//        row.forEach { print("$it ") }
        row.forEach { column ->

        }
    }
//    val rows = matrix.size
//    val columns = matrix[0].size
//    println("Matrix[R=$rows,C=$columns]")

//    val l1 = mutableListOf(0, 1, 1, 2)
//    val l2 = mutableListOf(0, 5, 0, 0)
//    val l3 = mutableListOf(2, 0, 3, 3)
//
////    l1.zip(l2).zip(l3).forEach {
////        println(it.toString().filter { it != '(' && it != ')' })
////    }
//
//
//    l1.zip(l2).zip(l3).forEach {
//        val s: String = it.toString().filter { it != '(' && it != ')' }
//    }

}

fun matrixElementsSum(matrix: MutableList<MutableList<Int>>): Int {
    return 0
}